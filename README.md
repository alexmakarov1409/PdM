# **Predictive Maintenance System**

Система предиктивного обслуживания промышленного оборудования на основе машинного обучения для прогнозирования отказов за 7 дней.

## 📊 **Ключевые показатели**

| Метрика | Значение | Цель | Статус |
|---------|----------|------|--------|
| **Recall** | 0.89 | ≥ 0.85 | ✅ Достигнуто |
| **False Positive Rate** | 0.008 | < 0.15 | ✅ Достигнуто |
| **Время инференса** | 3.46 мс | < 50 мс | ✅ Достигнуто |
| **Экономия** | 49.5 млн руб/год | ≥ 30 млн руб | ✅ Достигнуто |

## 🎯 **Цели проекта**

1. **Бизнес-цель:** Сокращение затрат на обслуживание и потерь от простоев на 30%+
2. **Техническая цель:** ML-модель с Recall ≥ 0.85 и FPR < 0.15
3. **Архитектурная цель:** Масштабируемая система с временем отклика < 50 мс

## 📁 **Структура проекта**

```
project/
├── data/                    # Данные
│   ├── raw/                # Сырые данные
│   ├── processed/          # Обработанные данные
│   └── samples/            # Примеры данных
│
├── notebooks/              # Jupyter ноутбуки
│   ├── 01_data_preparation.ipynb
│   ├── 02_baseline_model.ipynb
│   ├── 03_model_experiments.ipynb
│   └── 04_final_model.ipynb
│
├── src/                    # Исходный код
│   ├── data/               # Обработка данных
│   ├── models/             # ML модели
│   ├── utils/              # Утилиты
│   ├── etl_pipeline.py     # ETL пайплайн
│   ├── scripts/            # Скрипты генерации данных
│   └── api/                # API сервис│
│
├── sql/                    # Исходный код SQL
│
├── tests/                  # Тесты
│   ├── unit/               # Юнит-тесты
│   └── integration/        # Интеграционные тесты
│
├── deployment/             # Деплой
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── docer/              # Конфиги для docer
│
├── docs/                   # Документация
│   ├── architecture.png    # Диаграмма архитектуры
│   ├── report.pdf          # PDF отчет
│   └── presentation.pdf    # Презентация
│
├── .env.example            # Пример переменных окружения
├── .gitignore
├── requirements.txt        # Зависимости Python
└── README.md               # Этот файл
```

## 🚀 **Быстрый старт**

### **Предварительные требования**

- Python 3.9+
- Docker и Docker Compose (для контейнеризации)
- PostgreSQL 13+
- Redis 6+

### **Установка**

1. **Клонирование репозитория:**
```bash
git clone https://github.com/alexmakarov1409/PdM.git
cd PdM
```

2. **Создание виртуального окружения:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

4. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

### **Запуск локально**

**Вариант 1: Запуск через Docker (рекомендуется):**
```bash
docker-compose up --build
```

**Вариант 2: Запуск вручную:**

1. **Запуск базы данных:**
```bash
docker run --name postgres -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:13
docker run --name redis -p 6379:6379 -d redis:alpine
```

2. **Инициализация базы данных:**
```bash
python src/data/init_db.py
```

3. **Запуск API сервиса:**
```bash
uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
```

4. **Запуск ETL пайплайна:**
```bash
python src/etl_pipeline.py
```

### **Проверка работоспособности**

1. **Проверить здоровье API:**
```bash
curl http://localhost:8000/health
```

2. **Получить прогноз для оборудования:**
```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{"equipment_id": "M14860"}'
```

## 📈 **Работа с данными**

### **Датасет**

Используется датасет Predictive Maintenance Dataset с Kaggle:
- 10,000 записей о промышленном оборудовании
- 5 числовых параметров + 2 категориальных
- 6 типов отказов (включая "No Failure")

**Ссылка:** [Predictive Maintenance Dataset](https://www.kaggle.com/datasets/shivamb/machine-predictive-maintenance-classification)

### **EDA и предобработка**

Полный анализ данных доступен в ноутбуке `notebooks/01_data_preparation.ipynb`:

- Анализ дисбаланса классов (96.6% без отказов, 3.4% с отказами)
- Корреляционный анализ признаков
- Feature engineering (разность температур, отношения параметров)
- Обработка категориальных признаков

## 🤖 **Модели машинного обучения**

### **Используемые алгоритмы**

1. **Baseline:** Logistic Regression
2. **Random Forest**
3. **XGBoost**
4. **LightGBM** (выбран как финальная модель)
5. **SVM**
6. **SMOTE+LGB**
7. **Ensemble**

### **Метрики оценки**

- **Основные:** Recall ≥ 0.85, False Positive Rate < 0.15
- **Дополнительные:** Precision, F1-Score, AUC-ROC
- **Бизнес-метрики:** Экономия, ROI, сокращение простоев

### **Результаты**

| Модель | Recall | FPR | AUC-ROC | Время инференса |
|--------|--------|-----|---------|-----------------|
| Logistic Regression | 0.72 | 0.28 | 0.82 | < 1 мс |
| Random Forest | 0.72 | 0.001 | 0.98 | 12 мс |
| **LightGBM (финальная)** | **0.97** | **0.06** | **0.99** | **4.2 мс** |

## 🔧 **API Документация**

### **Эндпоинты**

**GET /health** - Проверка здоровья системы
```bash
curl http://localhost:8000/health
```

**POST /predict** - Прогноз вероятности отказа
```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{"equipment_id": "M14860"}'
```

**POST /predict_batch** - Пакетный прогноз
```bash
curl -X POST "http://localhost:8000/predict_batch" \
  -H "Content-Type: application/json" \
  -d '{"equipment_ids": ["M14860", "L47181", "H29643"]}'
```

**GET /model_info** - Информация о модели
```bash
curl http://localhost:8000/model_info
```

### **Пример ответа**

```json
{
  "equipment_id": "M14860",
  "failure_probability": 0.87,
  "prediction": true,
  "threshold": 0.35,
  "shap_explanation": {
    "tool_wear": 0.35,
    "torque": 0.22,
    "temp_diff": 0.15
  },
  "inference_time": 0.0042
}
```

## 🐳 **Деплой**

### **Docker**

```bash
# Сборка образа
docker build -t predictive-maintenance:latest .

# Запуск контейнера
docker run -p 8000:8000 --env-file .env predictive-maintenance:latest
```

### **Docker Compose**

```bash
# Запуск всего стека
docker-compose up --build

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f api
```

## 🧪 **Тестирование**

### **Запуск тестов**

```bash
# Все тесты
pytest tests/ -v

# Только юнит-тесты
pytest tests/unit/ -v

# Только интеграционные тесты
pytest tests/integration/ -v

# С покрытием кода
pytest tests/ --cov=src --cov-report=html
```

### **Нагрузочное тестирование**

```bash
# Установка locust
pip install locust

# Запуск нагрузочного теста
locust -f tests/load_test.py
```

## 📊 **Мониторинг**

### **Дашборды**

- **Grafana:** http://localhost:3000 (admin/admin)
- **MLflow UI:** http://localhost:5000
- **Evidently AI:** http://localhost:8085

### **Метрики Prometheus**

```
# Бизнес метрики
predictive_maintenance_business_savings_total
predictive_maintenance_false_positives_total
predictive_maintenance_recall_current

# Технические метрики
predictive_maintenance_inference_latency_seconds
predictive_maintenance_model_accuracy
predictive_maintenance_api_requests_total
```

## 📚 **Документация**

### **Полная документация**

- **Отчет (PDF):** [docs/report.pdf](docs/report.pdf)
- **Презентация:** [docs/presentation.pdf](docs/presentation.pdf)
- **Архитектура:** [docs/architecture.png](docs/architecture.png)

### **Генерация документации**

```bash
# Генерация документации API
python -m src.api.generate_openapi

# Сборка документации Sphinx
cd docs
make html
```

## 👥 **Команда проекта**

| Роль | Ответственность |
|------|-----------------|
| **Архитектор ИИ** | Общая архитектура, ML модели |
| **Data Engineer** | ETL пайплайны, Feature Store |
| **Backend Developer** | API, базы данных, инфраструктура |
| **DevOps Engineer** | Деплой, мониторинг, CI/CD |

## 📞 **Контакты и поддержка**

- **Issues:** [GitHub Issues](https://github.com/alexmakarov1409/PdM/issues)
- **Email:** alex_makarov@rambler.ru

## 📝 **Лицензия**

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).

---
**Статус проекта:** ✅ Готово к production внедрению  
**Последнее обновление:** Декабрь 2025  
**Версия:** 1.0.0